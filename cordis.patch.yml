- id: web
  config:
    searchProvider: modellix
    fetchProvider: modellix

- id: tool-web
  config:
    search: true
    fetch: true
    searchTimeoutMs: 60000
    fetchTimeoutMs: 60000

- insert:
    - id: modellix
      name: dsh-modellix
